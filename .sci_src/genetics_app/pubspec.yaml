name: dna_reading_activities
description: DNA and chromosomes reading comprehension - passage-based Q&A plus fill-in-the-blank sentences.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/dna_structure.jpg
    - assets/chromosomes.jpg
